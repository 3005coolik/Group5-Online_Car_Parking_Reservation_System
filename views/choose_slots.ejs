<!DOCTYPE html>
<html>
    <head>
        <title> Choose slots </title>
        <h1> Choose your preffered slot</h1>
    </head>

    <body>
    <br>
    <br>
    <form method="post" action="/user/<%=user_id%>/<%=parking._id%>/choose_slots">
        <% for(var i = 1; i <= number; i++) { %>
            <input id="slot"+i name="grp" type="radio"> slot <%= i %> </input>
            <% if(Unavailable.includes('i')) { %>
                <% var x = "slot"+i ; %>
                <% document.getElementById(x).disabled = true; %>
            <%   } %>
        <%  } %>
        

        
    <br>
    <button onclick=" " > submit </button>
    </form>
    </body>
    <!-- <script>
        
        var unavailable = [2,6,9,13,17,22,25,30,33,37];
        // var unavailable = /<%=Unavailable%>/ ;
        // document.write(unavailable[3]);
        for(var i=0;i<unavailable.length;i++)
        {
            var x="slot"+unavailable[i];
            // document.write(x);
            // document.write("\n");
            document.getElementById(x).disabled = true;    
        }
         
    </script> -->
</html>